"use server";
import { prisma } from "../db/client";

export async function saveCourseToProfile(courseCode: string, title: string) {
  return null;
}

export async function deleteSavedCourse(courseCode: string) {
  return null;
}

